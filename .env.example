# Comma-separated admin emails. Leave blank to deny all admin access.
# Local Sites preview's mock identity is seedy@sites.test.
# Set the actual owner's email separately in hosted environment variables.
ADMIN_EMAIL=
